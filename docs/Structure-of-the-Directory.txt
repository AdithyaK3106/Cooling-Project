cooling-system/
│
├── requirements.txt              ← RUNTIME only (psutil, pandas, sklearn, numpy, xgboost)
├── requirements-training.txt     ← Training deps (adds torch, torch-geometric for research GNN)
├── README.md
│
├── src/                          ← PRODUCTION RUNTIME — nothing in training/ overrides this
│   ├── features.py               ← FeatureProcessor: single source of truth for all feature logic
│   ├── inference.py              ← InferenceEngine: live prediction loop
│   ├── preprocess.py             ← Preprocessing pipeline (training-time data prep)
│   └── telemetry_logger.py       ← Telemetry collector (psutil + nvidia-smi)
│
├── training/                     ← TRAINING PIPELINE — never deployed to runtime
│   ├── data_processing.py        ← [REWRITE per tickets] Must import FeatureProcessor from src/features.py
│   ├── xgboost_model.py          ← ThermalRiskXGB trainer (fix hyperparams, output scale)
│   ├── inference_pipeline.py     ← End-to-end training validation (not production inference)
│   └── research/
│       └── gnn_model.py          ← ThermalGNN GraphSAGE — experimental only, per M-03
│
├── models/
│   ├── cooling_model.pkl         ← Trained XGBoost model (output of training pipeline)
│   └── preprocessor_state.pkl    ← FeatureProcessor log-scaling bounds (output of preprocess.py)
│
├── data/
│   ├── raw/
│   │   └── telemetry_data.csv
│   ├── processed/
│   │   ├── X.csv
│   │   ├── y.csv
│   │   └── processed_dataset.csv
│   └── inference_logs.csv        ← Written by inference.py at runtime
│
├── scripts/
│   ├── generate_test_data.py
│   ├── create_mock_model.py
│   └── train.py                  ← Entry point: runs data_processing → xgboost_model → saves pkl
│
└── docs/
    ├── PRD_FRD.docx
    ├── TELEMETRY_README.md
    ├── UNIFIED_PREPROCESSING.md
    └── ML_Gap_Analysis_and_Dev_Tickets.docx